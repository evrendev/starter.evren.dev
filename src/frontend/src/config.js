const config = {
  sidebarDrawer: true,
  customizerDrawer: false,
  miniSidebar: false,
  theme: "LightTheme",
  fontTheme: "Roboto",
  inputBg: false,
  defaultLocale: "tr",
  fallbackLocale: "en",
  languages: ["tr", "en", "de"]
};

export default config;
