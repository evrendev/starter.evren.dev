const config = {
  sidebarDrawer: true,
  customizerDrawer: false,
  miniSidebar: false,
  theme: "light",
  fontTheme: "Roboto",
  inputBg: false,
  defaultLocale: "tr",
  fallbackLocale: "en",
  languages: ["tr", "en", "de"],
  itemsPerPage: 25
};

export default config;
